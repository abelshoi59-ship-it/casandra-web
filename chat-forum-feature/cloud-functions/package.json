{
  "name": "casandra-ai-assistant-functions",
  "version": "1.0.0",
  "description": "Cloud Functions fronting the Claude API for the Casandra AI assistant.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:aiAssistantStream"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
